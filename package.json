{
  "private": true,
  "name": "distance-component",
  "homepage": "https://Waterwheelss.github.io/distance-component",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build --no-prerender --no-sw",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch",
    "lint": "eslint src",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "preact"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^7.17.0",
    "eslint-config-preact": "^1.1.3",
    "preact-cli": "^3.0.0",
    "sirv-cli": "^1.0.3"
  },
  "dependencies": {
    "gh-pages": "^3.2.3",
    "preact": "^10.1.0",
    "preact-render-to-string": "^5.1.2",
    "react-lineto": "^3.2.1",
    "react-transition-group": "^4.4.2"
  }
}
